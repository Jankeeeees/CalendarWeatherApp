package com.example.calendarweatherapp.ui.viewmodel

import androidx.lifecycle.ViewModel
// import com.example.calendarweatherapp.data.repository.EventRepository

// If this ViewModel is used for "Events" (weather cache interactions specific to UI elements),
// it would be structured similarly to MainViewModel.
// For now, as per "no events" and structure, it's a placeholder.
class EventViewModel(
    // private val eventRepository: EventRepository
) : ViewModel() {
    // Example:
    // private val _events = MutableStateFlow<List<Event>>(emptyList())
    // val events: StateFlow<List<Event>> = _events.asStateFlow()

    // fun loadEventsForDate(date: LocalDate) {
    //     viewModelScope.launch {
    //         _events.value = eventRepository.getEventsForDate(date)
    //     }
    // }
}